import React from "react"
import {ImageBlock} from "./UI"
import PartnersImage from "@/assets/images/partners.png"

const Partners = ({id}) => {
  return (
    <ImageBlock image={PartnersImage} />
  )
}

export default Partners
