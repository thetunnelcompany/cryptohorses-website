import React from "react"
import {ImageBlock} from "./UI"
import RoadmapImage from "@/assets/images/roadmap.png"

const Roadmap = ({id}) => {
  return (
    <ImageBlock image={RoadmapImage} />
  )
}

export default Roadmap
